[English](../README.md) · [العربية](README.ar.md) · [Español](README.es.md) · [Français](README.fr.md) · [日本語](README.ja.md) · [한국어](README.ko.md) · [Tiếng Việt](README.vi.md) · [中文 (简体)](README.zh-Hans.md) · [中文（繁體）](README.zh-Hant.md) · [Deutsch](README.de.md) · [Русский](README.ru.md)



# SoraRemote

![Python](https://img.shields.io/badge/Python-3.10%2B-3776AB?logo=python&logoColor=white)
![Platform](https://img.shields.io/badge/Platform-Linux%20%7C%20macOS%20%7C%20WSL-6c757d)
![Server](https://img.shields.io/badge/Server-Tornado%20API-0EA5E9)
![Frontend](https://img.shields.io/badge/Frontend-PWA-10B981)
![Status](https://img.shields.io/badge/Status-Experimental-F59E0B)

SoraRemote هي عدة أدوات خفيفة مبنية على Python + Selenium لأتمتة واجهة Sora على الويب.

تدعم ثلاثة مسارات عمل متكاملة:
1. وكيل أتمتة CLI (`agents/sora_agent.py`) لكتابة الموجهات وتنفيذ إجراءات التحكم في الواجهة.
2. أداة تنزيل CLI (`agents/sora_download.py`) لاكتشاف الوسائط القابلة للتنزيل وتنزيلها.
3. خادم تحكم محلي Tornado + تطبيق PWA (`server/app.py` + `pwa/`) للتحكم عبر API ومن المتصفح.

تم الحفاظ على محتوى README الحالي كمرجع تشغيل أساسي مع إعادة تنظيمه لتحسين الوضوح.

## ✨ نظرة عامة

التصميم الأساسي:
- الاتصال بجلسة Chrome دائمة عبر DevTools remote debugging (المنفذ الافتراضي `9333`).
- إعادة استخدام حالة ملف تعريف المتصفح للحفاظ على استمرار تسجيل الدخول/الجلسة.
- أتمتة إجراءات المؤلف الأساسية (كتابة، plus/إرفاق وسائط، storyboard، settings، create).
- إتاحة نفس الإجراءات عبر REST + سجل WebSocket للتحكم من PWA محلي.

### لقطة سريعة لمسارات العمل

| مسار العمل | نقطة الدخول | الاستخدام الأساسي |
| --- | --- | --- |
| وكيل CLI | `agents/sora_agent.py` | كتابة الموجهات، الضغط على عناصر التحكم، أتمتة تدفق الإنشاء |
| أداة تنزيل CLI | `agents/sora_download.py` | اكتشاف الوسائط القابلة للتنزيل وحفظ الملفات محليًا |
| API + PWA | `server/app.py` + `pwa/` | تحكم عن بُعد وتنسيق بصري من المتصفح |

## ✅ الميزات

- تدفق الاتصال/تشغيل Chrome مع ملف تعريف قابل لإعادة الاستخدام (`--debugger-port`, `--start-chrome`, `--user-data-dir`).
- نقرات آمنة أو قسرية لعناصر التحكم الأساسية (`plus`, `storyboard`, `settings`, `create`, `profile`).
- كتابة الموجهات مع سلوك احتياطي للمحددات.
- إرفاق الوسائط عبر مسار ملف باستخدام حقن DataTransfer.
- تعبئة مشاهد Storyboard + تحديثات النص + إرفاق وسائط خاصة بـ Storyboard.
- أتمتة الإعدادات لـ model/orientation/duration/resolution.
- تدفق منفصل لاكتشاف التنزيلات وجلبها باستخدام cookies المتصفح.
- Tornado REST API وبث حي لسجل التصحيح عبر WebSocket.
- PWA محلي قابل للتثبيت مع الرفع، المعاينة، وعناصر تحكم دقيقة.

## 🗂️ بنية المشروع

```text
SoraRemote/
├─ README.md
├─ requirements.txt
├─ .github/
│  └─ FUNDING.yml
├─ agents/
│  ├─ sora_agent.py
│  └─ sora_download.py
├─ server/
│  └─ app.py
├─ pwa/
│  ├─ index.html
│  ├─ app.js
│  ├─ styles.css
│  ├─ manifest.webmanifest
│  └─ sw.js
├─ bin/
│  ├─ sora_type.sh
│  └─ sora_download.sh
├─ i18n/
│  └─ (يتضمن الآن ملفات ترجمة README)
├─ uploads/
│  └─ .gitkeep
└─ selenium_template -> ../auto-publish/ (symlink)
```

## 🧩 المتطلبات المسبقة

- Python 3.10+ (موصى به).
- تثبيت Chrome/Chromium وإمكانية تشغيله.
- شاشة للاستخدام غير headless (`--no-headless`) عندما يلزم تسجيل الدخول أو التفاعل اليدوي مع الواجهة.
- صلاحية الوصول إلى حساب Sora ضمن ملف تعريف Chrome المتصل.

## 📦 التثبيت

تدفق الإعداد الحالي من README الأساسي:

```bash
conda activate agent
pip install -r requirements.txt
```

التبعيات في `requirements.txt`:

| الحزمة | نطاق الإصدار |
| --- | --- |
| `selenium` | `>=4.17.2` |
| `tornado` | `>=6.4` |
| `Pillow` | `>=9.4.0` |
| `pillow-heif` | `>=0.16.0` |

## 🚀 الاستخدام

### بدء سريع (وكيل CLI)

بدء سريع (يفتح Sora في متصفح مُدار):

```bash
python agents/sora_agent.py
```

الاتصال بـ Chrome مع جلسة دائمة (موصى به لـ Sora):

```bash
python -m agents.sora_agent --debugger-port 9333 --start-chrome --no-headless --login-timeout 600 --text "A sunset over Tokyo, cinematic."
```

ملاحظات:
- تُفتح نافذة Chrome على صفحة Sora. إذا تمت إعادة التوجيه إلى تسجيل الدخول، سجّل الدخول؛ ينتظر السكربت ثم يكتب الموجّه.
- لإعادة استخدام نفس تسجيل الدخول، مرّر مسار ملف تعريف ثابت:

```bash
python -m agents.sora_agent --debugger-port 9333 --start-chrome --no-headless --user-data-dir "$HOME/chrome_sora_profile_9333"
```

### خيارات CLI الأساسية (`agents/sora_agent.py`)

- `--url` الصفحة المستهدفة (الافتراضي: `https://sora.chatgpt.com/explore`).
- `--debugger-port` للاتصال بـ Chrome موجود تم تشغيله باستخدام `--remote-debugging-port=PORT`.
- `--start-chrome` عند استخدامه مع `--debugger-port`، يقوم بتشغيل Chrome لك (مع `--user-data-dir`).
- `--no-headless` لتشغيل متصفح مرئي؛ ضروري لتسجيل الدخول وCloudflare.
- `--selector` محدد CSS لتحديد حقل الإدخال (الافتراضي يطابق textarea الخاص بمؤلف Sora).
- `--text` النص المراد كتابته في حقل الإدخال.
- `--chrome-binary` لتحديد مسار Chrome/Chromium بشكل صريح.
- `--action` إجراءات الواجهة: `list`, `plus`, `storyboard`, `settings`, `create`, `profile`.
- `--force-click` ينفّذ النقر حتى لو بدا العنصر معطّلًا.
- `--login-timeout` مدة الانتظار لإكمال المصادقة اليدوية.

التعامل مع Driver:
- يقوم الوكيل بإزالة أي `chromedriver` قديم من `PATH` قبل التشغيل.
- ثم يقوم Selenium Manager تلقائيًا باختيار Driver مطابق لإصدار Chrome المثبّت.

### أمثلة CLI (عناصر التحكم في الواجهة)

سرد عناصر التحكم الشائعة والنقر عليها:

```bash
python -m agents.sora_agent --debugger-port 9333 --no-headless --action list --action storyboard --action settings --action plus
```

نقرة قسرية على زر Create video (حتى لو كان معطّلًا):

```bash
python -m agents.sora_agent --debugger-port 9333 --no-headless --action create --force-click
```

فتح profile/settings والتنقل يدويًا عند الحاجة:

```bash
python -m agents.sora_agent --debugger-port 9333 --no-headless --action list --action profile
```

إذا لم يتم اكتشاف `profile`، فعادةً يفتح زر `settings` نفس القائمة.

### مسار التنزيل

اكتشف ونزّل الفيديوهات باستخدام سكربت المعالجة:

- تشغيل تجريبي (عرض المرشحين فقط): `./bin/sora_download.sh --dry-run`
- تنزيل حتى ملفين إلى `./downloads/sora`: `./bin/sora_download.sh --max 2`
- تغيير مجلد الإخراج: `OUT_DIR=$HOME/Videos/sora ./bin/sora_download.sh --max 1`

يتوفر الاستخدام المباشر للوحدة أيضًا عبر `python -m agents.sora_download ...`.

## 🌐 خادم التحكم + PWA

شغّل خادم Tornado:

```bash
python server/app.py
# listens on http://0.0.0.0:8791 and serves the PWA at /
```

افتراضيًا، الخادم:
- يعيد استخدام Chrome على منفذ remote debugging `9333`.
- يخزّن الملفات المرفوعة في `./uploads` ما لم يتم ضبط `SORA_UPLOADS_DIR`.

### نقاط النهاية الأساسية

كل نقاط النهاية تعمل على Chrome المتصل حاليًا (الافتراضي منفذ debugger `9333`).

| Method | Path | Payload | الوصف |
| --- | --- | --- | --- |
| `GET` | `/api/status` | none | يعيد حالة جاهزية DevTools والمنفذ النشط. |
| `POST` | `/api/open` | `{ url? }` | يوجّه تبويب Chrome المتصل إلى الرابط المحدد (الافتراضي Sora Explore). |
| `GET` | `/api/actions` | none | يفحص حالة الأزرار/عناصر التحكم (بيانات found/displayed/disabled). |
| `POST` | `/api/click` | `{ key, force? }` | ينفّذ ضغطة على عنصر تحكم واحد حيث `key ∈ {plus, storyboard, settings, create, profile}`. |
| `POST` | `/api/type` | `{ text, selector?, url? }` | يكتب نص الموجّه داخل محدد composer. |
| `POST` | `/api/compose` | `{ text, click_create? }` | يفتح صفحة الإنشاء، يكتب النص، ويضغط create اختياريًا. |
| `POST` | `/api/attach` | `{ path, click_plus? }` | يرفع الوسائط عبر DataTransfer injection؛ ويمسح الوسائط الحالية تلقائيًا (`click_plus` الافتراضي `false`). |
| `POST` | `/api/describe` | `{ text }` | يملأ textarea الخاص بـ “Optionally describe your video…”. |
| `POST` | `/api/script-updates` | `{ text }` | يملأ حقل “Describe updates to your script…”. |
| `POST` | `/api/storyboard` | `{ scenes: ["scene 1", ...], script_updates?: "...", media_path?: "..." }` | يفتح storyboard، يملأ نصوص المشاهد، ويطبّق اختياريًا تحديثات النص ووسائط storyboard. |
| `POST` | `/api/storyboard-media` | `{ path }` | يرفق وسائط بمُحمّل storyboard المخصص عندما يكون storyboard ظاهرًا مسبقًا. |
| `POST` | `/api/storyboard-attach-only` | `{ path }` | يضمن فتح storyboard ثم يرفق الوسائط. |
| `POST` | `/api/settings` | `{ model?, orientation?, duration?, resolution? }` | يفتح الإعدادات ويطبّق القيم المحددة؛ وتعيد الاستجابة التسميات المطبّقة. |
| `POST` | `/api/upload` | multipart form data | يحفظ الملف/الملفات المحلية في مجلد رفع الخادم ويعيد المسارات على الخادم. |
| `POST` | `/api/preview` | multipart form data | يحوّل الصورة إلى معاينة PNG (مفيد كحل احتياطي HEIC/HEIF/AVIF في الواجهة). |
| `GET` | `/ws` | WebSocket | يبث أحداث الإجراء/التصحيح. |

### عناصر تحكم PWA

افتح `http://0.0.0.0:8791` (أو المضيف الذي اخترته) بعد تشغيل `server/app.py`.

أبرز النقاط من التنفيذ الحالي:
- رفع وسائط عبر منتقي الملفات أو لصق مسار، ثم الضغط على **Plus** للإرفاق دون إعادة فتح نوافذ اختيار النظام.
- تطبيق وصف الوسائط في صندوق “Media description” المخصص.
- عناصر تحكم مستقلة لـ **Set Model** و**Set Orientation** و**Set Duration** و**Set Resolution** وتحديثات النص.
- عناصر تحكم Storyboard للمشاهد، تحديثات النص، فتح لوحة storyboard، وإرفاق مسار storyboard الحالي.
- سجل تصحيح مباشر يعرض استدعاءات API والقيم المرتجعة من Sora (مثل model/duration المحددَين).

## ⚙️ الإعداد

### متغيرات البيئة

`server/app.py` يقرأ:
- `SORA_DEBUGGER_PORT` (الافتراضي `9333`)
- `SORA_USER_DATA_DIR` (الافتراضي `~/chrome_sora_profile_<port>`)
- `SORA_DISPLAY` (عرض X اختياري)
- `SORA_API_PORT` (الافتراضي `8791`)
- `SORA_URL` (الافتراضي `https://sora.chatgpt.com/explore`)
- `SORA_UPLOADS_DIR` (تجاوز اختياري لمجلد الرفع)

`agents/sora_agent.py` يدعم أيضًا:
- `CHROME_BINARY` (إذا لم يتم تمرير `--chrome-binary`)

سكربتات التغليف تدعم:
- `PORT`, `SORA_PROFILE_DIR`, `TIMEOUT`, `LOGIN_TIMEOUT` (`bin/sora_type.sh`)
- `PORT`, `SORA_PROFILE_DIR`, `OUT_DIR` (`bin/sora_download.sh`)

## 🧪 أمثلة

### مثال API كامل (curl)

```bash
# 1) افتح Sora
curl -s -X POST http://127.0.0.1:8791/api/open -H 'Content-Type: application/json' -d '{}'

# 2) اكتب الموجّه
curl -s -X POST http://127.0.0.1:8791/api/type -H 'Content-Type: application/json' -d '{"text":"A cinematic drone shot over snowy mountains."}'

# 3) اضبط model و duration
curl -s -X POST http://127.0.0.1:8791/api/settings -H 'Content-Type: application/json' -d '{"model":"sora 2 pro"}'
curl -s -X POST http://127.0.0.1:8791/api/settings -H 'Content-Type: application/json' -d '{"duration":15}'

# 4) اضغط Create
curl -s -X POST http://127.0.0.1:8791/api/click -H 'Content-Type: application/json' -d '{"key":"create"}'
```

### رفع الوسائط + إرفاقها عبر API

```bash
# ارفع الملف واحصل على مسار الخادم
curl -s -X POST http://127.0.0.1:8791/api/upload -F 'file=@/absolute/path/to/input.jpg'

# ثم أرفِق باستخدام المسار المُعاد
curl -s -X POST http://127.0.0.1:8791/api/attach \
  -H 'Content-Type: application/json' \
  -d '{"path":"/absolute/or/server-returned/path.jpg","click_plus":false}'
```

## 🛠️ ملاحظات التطوير

- لا توجد حاليًا وحدة حزم جاهزة (`pyproject.toml`/`setup.py` غير موجودين).
- لا يوجد حاليًا مسار CI/test/lint في لقطة المستودع هذه.
- `selenium_template` هو symlink إلى `../auto-publish/`؛ محتوى الهدف خارج هذا المستودع.
- يشير PWA manifest إلى `/icons/icon-192.png` و`/icons/icon-512.png`؛ أصول الأيقونات غير متتبعة حاليًا في هذا المستودع.

## 🧯 استكشاف الأخطاء وإصلاحها

- فشل الاتصال بـ Chrome:
  - تأكد من تشغيل Chrome مع `--remote-debugging-port=9333` (أو منفذ مطابق لـ `--debugger-port`).
  - افحص `GET /api/status` للتأكد من `devtools_ready: true`.
- تكرار طلبات تسجيل الدخول:
  - استخدم `--user-data-dir` دائمًا وتجنّب مسارات ملفات تعريف عشوائية.
- عدم تقدم مسار Cloudflare/تسجيل الدخول:
  - شغّل بدون headless (`--no-headless`) وزد قيمة `--login-timeout`.
- إرفاق الوسائط لا يفعل شيئًا:
  - أكّد أن مسار الملف موجود على جهاز الخادم، واستخدم `/api/upload` + المسار المُعاد عند الشك.
- فشل إرفاق وسائط Storyboard:
  - جرّب `POST /api/storyboard-attach-only` أو افتح storyboard أولًا ثم `/api/storyboard-media`.
- عنصر تحكم الدقة غير متاح في PWA:
  - دقة `High` مفعّلة فقط عندما يكون model هو `Sora 2 Pro`.
- مشاكل chromedriver الخاطئ:
  - أزل أي chromedriver مثبت يدويًا من إعدادات shell لديك؛ هذا المشروع يعتمد عمدًا على Selenium Manager لاختيار النسخ المطابقة.

## 🧭 خارطة الطريق

تحسينات متوقعة/مخطط لها لاحقًا:
- إضافة اختبارات آلية لاستقرار المحددات ومعالجات API.
- إضافة أدوات lint/format ومسارات CI.
- إضافة أصول أيقونات PWA المتتبعة واستراتيجية أقوى للتخزين المؤقت دون اتصال.
- إضافة ملفات README متعددة اللغات بشكل رسمي تحت `i18n/`.
- إضافة بيانات الحزم لتسهيل التثبيت.

## 🤝 المساهمة

المساهمات مرحّب بها.

العملية المقترحة:
1. اعمل Fork وأنشئ فرع ميزة.
2. اجعل التغييرات محددة النطاق وضمّن ملاحظات إعادة الإنتاج/الاستخدام لتغييرات أتمتة الواجهة.
3. تحقّق من التدفقات يدويًا مع جلسة Chrome متصلة فعلية.
4. افتح PR مع تفاصيل السلوك قبل/بعد.

إذا غيّرت المحددات أو منطق التفاعل، ضمّن سياقًا واضحًا من واجهة Sora لتسهيل تشخيص الانحدارات.

## ❤️ الدعم / الرعاية

روابط التمويل من `.github/FUNDING.yml`:
- GitHub Sponsors: https://github.com/sponsors/lachlanchen
- روابط المشروع: https://lazying.art, https://chat.lazying.art, https://onlyideas.art

## 🙏 شكر وتقدير

- Selenium وSelenium Manager لأتمتة المتصفح وحل توافق الـ driver.
- Tornado لخدمة التحكم الخفيفة غير المتزامنة عبر HTTP/WebSocket.
- Pillow و`pillow-heif` لدعم تحويل/معاينة الصور محليًا.

## 🧱 نسخة مرجعية مستقرة

إذا كنت تحتاج خط أساس مستقرًا يضمن عمل إرفاق وسائط Storyboard طرفًا لطرف (بما في ذلك زري Open Storyboard / Attach Current Path وتدفق Apply الموحّد)، ارجع إلى commit التالي:

`c6683ed6d9ee0ac110536352867a26a966e3e275`

## 📄 الترخيص

لا يوجد ملف ترخيص حاليًا في لقطة هذا المستودع (تم التحقق ضمن هذه الصياغة بتاريخ **February 28, 2026**).

افتراض: جميع الحقوق تظل لمالك المستودع حتى تتم إضافة ترخيص. إذا لم يكن ذلك مقصودًا، أضف ملف `LICENSE` وحدّث هذا القسم.
