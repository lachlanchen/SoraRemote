[English](../README.md) · [العربية](README.ar.md) · [Español](README.es.md) · [Français](README.fr.md) · [日本語](README.ja.md) · [한국어](README.ko.md) · [Tiếng Việt](README.vi.md) · [中文 (简体)](README.zh-Hans.md) · [中文（繁體）](README.zh-Hant.md) · [Deutsch](README.de.md) · [Русский](README.ru.md)



# SoraRemote

![Python](https://img.shields.io/badge/Python-3.10%2B-3776AB?logo=python&logoColor=white)
![Platform](https://img.shields.io/badge/Platform-Linux%20%7C%20macOS%20%7C%20WSL-6c757d)
![Server](https://img.shields.io/badge/Server-Tornado%20API-0EA5E9)
![Frontend](https://img.shields.io/badge/Frontend-PWA-10B981)
![Status](https://img.shields.io/badge/Status-Experimental-F59E0B)

SoraRemote — это легковесный toolkit на Python + Selenium для автоматизации веб-интерфейса Sora.

Поддерживаются три взаимодополняющих рабочих потока:
1. CLI-агент автоматизации (`agents/sora_agent.py`) для ввода промптов и действий управления UI.
2. CLI-загрузчик (`agents/sora_download.py`) для поиска и скачивания медиакандидатов.
3. Локальный сервер управления Tornado + PWA (`server/app.py` + `pwa/`) для API-управления и управления через браузер.

Текущее содержимое README сохранено как каноническое операционное руководство и реорганизовано для большей ясности.

## ✨ Обзор

Базовый дизайн:
- Подключение к постоянной сессии Chrome через удаленную отладку DevTools (порт по умолчанию `9333`).
- Повторное использование состояния профиля браузера для сохранения непрерывности логина/сессии.
- Автоматизация ключевых действий композера (ввод, attach через plus/media, storyboard, settings, create).
- Публикация тех же действий через REST + логи WebSocket для локального PWA-контроллера.

### Снимок рабочих потоков

| Рабочий поток | Точка входа | Основное применение |
| --- | --- | --- |
| CLI Agent | `agents/sora_agent.py` | Вводить промпты, нажимать элементы управления, автоматизировать compose-flow |
| CLI Downloader | `agents/sora_download.py` | Искать скачиваемые медиа и сохранять файлы локально |
| API + PWA | `server/app.py` + `pwa/` | Удаленное управление и визуальная оркестрация из браузера |

## ✅ Возможности

- Поток attach/start Chrome с переиспользуемым профилем (`--debugger-port`, `--start-chrome`, `--user-data-dir`).
- Безопасные или принудительные клики по ключевым контролам (`plus`, `storyboard`, `settings`, `create`, `profile`).
- Ввод промпта с fallback-поведением по селекторам.
- Прикрепление медиа по пути файла с DataTransfer injection.
- Заполнение сцен storyboard + обновления script + прикрепление медиа в storyboard.
- Автоматизация settings для model/orientation/duration/resolution.
- Раздельный поток поиска загрузок и скачивания с использованием cookies браузера.
- Tornado REST API и live WebSocket debug stream.
- Локально устанавливаемый PWA с загрузкой, предпросмотром и детальными контролами.

## 🗂️ Структура проекта

```text
SoraRemote/
├─ README.md
├─ requirements.txt
├─ .github/
│  └─ FUNDING.yml
├─ agents/
│  ├─ sora_agent.py
│  └─ sora_download.py
├─ server/
│  └─ app.py
├─ pwa/
│  ├─ index.html
│  ├─ app.js
│  ├─ styles.css
│  ├─ manifest.webmanifest
│  └─ sw.js
├─ bin/
│  ├─ sora_type.sh
│  └─ sora_download.sh
├─ i18n/
│  └─ (сейчас пусто)
├─ uploads/
│  └─ .gitkeep
└─ selenium_template -> ../auto-publish/ (symlink)
```

## 🧩 Требования

- Python 3.10+ (рекомендуется).
- Установленный и запускаемый Chrome/Chromium.
- Дисплей для запуска не в headless-режиме (`--no-headless`), когда нужен логин или интерактивный UI.
- Доступ к аккаунту Sora в подключенном профиле Chrome.

## 📦 Установка

Существующий setup-flow из канонического README:

```bash
conda activate agent
pip install -r requirements.txt
```

Зависимости в `requirements.txt`:

| Пакет | Спецификация версии |
| --- | --- |
| `selenium` | `>=4.17.2` |
| `tornado` | `>=6.4` |
| `Pillow` | `>=9.4.0` |
| `pillow-heif` | `>=0.16.0` |

## 🚀 Использование

### Быстрый старт (CLI-агент)

Быстрый старт (открывает Sora в управляемом браузере):

```bash
python agents/sora_agent.py
```

Подключение к Chrome с постоянной сессией (рекомендуется для Sora):

```bash
python -m agents.sora_agent --debugger-port 9333 --start-chrome --no-headless --login-timeout 600 --text "A sunset over Tokyo, cinematic."
```

Примечания:
- Откроется окно Chrome на странице Sora. Если будет переход на логин, выполните вход; скрипт подождет и затем введет ваш промпт.
- Чтобы переиспользовать тот же логин, передайте фиксированный путь профиля:

```bash
python -m agents.sora_agent --debugger-port 9333 --start-chrome --no-headless --user-data-dir "$HOME/chrome_sora_profile_9333"
```

### Ключевые CLI-опции (`agents/sora_agent.py`)

- `--url` целевая страница (по умолчанию: `https://sora.chatgpt.com/explore`).
- `--debugger-port` подключение к уже запущенному Chrome с `--remote-debugging-port=PORT`.
- `--start-chrome` при использовании с `--debugger-port` запускает Chrome за вас (с `--user-data-dir`).
- `--no-headless` запуск видимого браузера; необходим для логина и Cloudflare.
- `--selector` CSS для поиска поля ввода (по умолчанию соответствует textarea композера Sora).
- `--text` что вводить в поле.
- `--chrome-binary` явное указание пути к Chrome/Chromium.
- `--action` действия UI: `list`, `plus`, `storyboard`, `settings`, `create`, `profile`.
- `--force-click` кликает, даже если элемент выглядит отключенным.
- `--login-timeout` окно ожидания для ручного завершения аутентификации.

Обработка драйвера:
- Перед запуском агент удаляет любой устаревший `chromedriver` из `PATH`.
- Затем Selenium Manager автоматически подбирает соответствующий драйвер для установленного Chrome.

### Примеры CLI (элементы управления UI)

Вывести список и нажать распространенные элементы управления:

```bash
python -m agents.sora_agent --debugger-port 9333 --no-headless --action list --action storyboard --action settings --action plus
```

Принудительно нажать кнопку Create video (даже если она disabled):

```bash
python -m agents.sora_agent --debugger-port 9333 --no-headless --action create --force-click
```

Открыть profile/settings и при необходимости перейти вручную:

```bash
python -m agents.sora_agent --debugger-port 9333 --no-headless --action list --action profile
```

Если `profile` не обнаружен, кнопка `settings` обычно открывает то же меню.

### Поток загрузчика

Поиск и скачивание видео с помощью скрипта-обертки:

- Dry-run (только список кандидатов): `./bin/sora_download.sh --dry-run`
- Скачать до 2 файлов в `./downloads/sora`: `./bin/sora_download.sh --max 2`
- Изменить выходную папку: `OUT_DIR=$HOME/Videos/sora ./bin/sora_download.sh --max 1`

Доступно и прямое использование модуля: `python -m agents.sora_download ...`.

## 🌐 Сервер управления + PWA

Запуск Tornado-сервера:

```bash
python server/app.py
# listens on http://0.0.0.0:8791 and serves the PWA at /
```

По умолчанию сервер:
- Переиспользует Chrome на порту удаленной отладки `9333`.
- Хранит загрузки в `./uploads`, если не задан `SORA_UPLOADS_DIR`.

### Ключевые эндпоинты

Все эндпоинты работают с текущим подключенным Chrome (по умолчанию порт отладки `9333`).

| Method | Path | Payload | Description |
| --- | --- | --- | --- |
| `GET` | `/api/status` | none | Возвращает состояние готовности DevTools и активный порт. |
| `POST` | `/api/open` | `{ url? }` | Переходит в подключенной вкладке Chrome на указанный URL (по умолчанию Sora Explore). |
| `GET` | `/api/actions` | none | Проверяет состояние кнопок/контролов (метаданные found/displayed/disabled). |
| `POST` | `/api/click` | `{ key, force? }` | Нажимает один контрол, где `key ∈ {plus, storyboard, settings, create, profile}`. |
| `POST` | `/api/type` | `{ text, selector?, url? }` | Вводит текст промпта в selector композера. |
| `POST` | `/api/compose` | `{ text, click_create? }` | Открывает страницу compose, вводит текст, опционально нажимает create. |
| `POST` | `/api/attach` | `{ path, click_plus? }` | Загружает медиа через DataTransfer injection; автоматически очищает существующее медиа (`click_plus` по умолчанию `false`). |
| `POST` | `/api/describe` | `{ text }` | Заполняет textarea “Optionally describe your video…”. |
| `POST` | `/api/script-updates` | `{ text }` | Заполняет поле “Describe updates to your script…”. |
| `POST` | `/api/storyboard` | `{ scenes: ["scene 1", ...], script_updates?: "...", media_path?: "..." }` | Открывает storyboard, заполняет текстовые поля сцен, опционально применяет script updates и storyboard media. |
| `POST` | `/api/storyboard-media` | `{ path }` | Прикрепляет медиа к storyboard-специфичному uploader, когда storyboard уже открыт. |
| `POST` | `/api/storyboard-attach-only` | `{ path }` | Гарантирует, что storyboard открыт, затем прикрепляет медиа. |
| `POST` | `/api/settings` | `{ model?, orientation?, duration?, resolution? }` | Открывает settings и применяет выбранные значения; ответ возвращает примененные labels. |
| `POST` | `/api/upload` | multipart form data | Сохраняет локальные файл(ы) в каталог загрузок сервера и возвращает серверные пути. |
| `POST` | `/api/preview` | multipart form data | Конвертирует изображение в PNG-preview (полезно для fallback HEIC/HEIF/AVIF в UI). |
| `GET` | `/ws` | WebSocket | Передает поток action/debug-событий. |

### Элементы управления PWA

После запуска `server/app.py` откройте `http://0.0.0.0:8791` (или выбранный хост).

Основные возможности текущей реализации:
- Загружайте медиа через file picker или вставкой пути, затем нажмите **Plus**, чтобы прикрепить без повторного открытия системных диалогов выбора файла.
- Применяйте описание медиа в отдельном поле “Media description”.
- Независимые контролы для **Set Model**, **Set Orientation**, **Set Duration**, **Set Resolution** и обновлений скрипта.
- Storyboard-контролы для сцен, обновлений скрипта, открытия панели storyboard и прикрепления текущего storyboard path.
- Live debug log, показывающий API-вызовы и возвращенные Sora значения (например выбранные model/duration).

## ⚙️ Конфигурация

### Переменные окружения

`server/app.py` читает:
- `SORA_DEBUGGER_PORT` (по умолчанию `9333`)
- `SORA_USER_DATA_DIR` (по умолчанию `~/chrome_sora_profile_<port>`)
- `SORA_DISPLAY` (необязательный X display)
- `SORA_API_PORT` (по умолчанию `8791`)
- `SORA_URL` (по умолчанию `https://sora.chatgpt.com/explore`)
- `SORA_UPLOADS_DIR` (необязательное переопределение каталога загрузок)

`agents/sora_agent.py` также поддерживает:
- `CHROME_BINARY` (если `--chrome-binary` не передан)

Скрипты-обертки поддерживают:
- `PORT`, `SORA_PROFILE_DIR`, `TIMEOUT`, `LOGIN_TIMEOUT` (`bin/sora_type.sh`)
- `PORT`, `SORA_PROFILE_DIR`, `OUT_DIR` (`bin/sora_download.sh`)

## 🧪 Примеры

### Сквозной API-пример (curl)

```bash
# 1) Open Sora
curl -s -X POST http://127.0.0.1:8791/api/open -H 'Content-Type: application/json' -d '{}'

# 2) Type prompt
curl -s -X POST http://127.0.0.1:8791/api/type -H 'Content-Type: application/json' -d '{"text":"A cinematic drone shot over snowy mountains."}'

# 3) Set model and duration
curl -s -X POST http://127.0.0.1:8791/api/settings -H 'Content-Type: application/json' -d '{"model":"sora 2 pro"}'
curl -s -X POST http://127.0.0.1:8791/api/settings -H 'Content-Type: application/json' -d '{"duration":15}'

# 4) Click Create
curl -s -X POST http://127.0.0.1:8791/api/click -H 'Content-Type: application/json' -d '{"key":"create"}'
```

### Загрузка медиа + attach через API

```bash
# Upload file and get server path
curl -s -X POST http://127.0.0.1:8791/api/upload -F 'file=@/absolute/path/to/input.jpg'

# Then attach using returned path
curl -s -X POST http://127.0.0.1:8791/api/attach \
  -H 'Content-Type: application/json' \
  -d '{"path":"/absolute/or/server-returned/path.jpg","click_plus":false}'
```

## 🛠️ Заметки по разработке

- Сейчас нет упакованного модуля (`pyproject.toml`/`setup.py` отсутствуют).
- В текущем снимке репозитория нет CI/test/lint-пайплайна.
- `selenium_template` — это symlink на `../auto-publish/`; содержимое целевой директории находится вне этого репозитория.
- PWA manifest ссылается на `/icons/icon-192.png` и `/icons/icon-512.png`; assets иконок сейчас не отслеживаются в этом репозитории.

## 🧯 Устранение неполадок

- Chrome не подключается:
  - Убедитесь, что Chrome запущен с `--remote-debugging-port=9333` (или соответствующим `--debugger-port`).
  - Проверьте `GET /api/status` на `devtools_ready: true`.
- Повторяющиеся запросы логина:
  - Используйте постоянный `--user-data-dir` и избегайте случайных путей профиля.
- Поток Cloudflare/логина не продолжается:
  - Запустите без headless (`--no-headless`) и увеличьте `--login-timeout`.
- Прикрепление медиа ничего не делает:
  - Убедитесь, что путь к файлу существует на серверной машине; при сомнениях используйте `/api/upload` + возвращенный путь.
- Не удается прикрепить медиа в storyboard:
  - Попробуйте `POST /api/storyboard-attach-only` или сначала откройте storyboard, затем `/api/storyboard-media`.
- В PWA недоступен выбор resolution:
  - Resolution `High` доступно только при модели `Sora 2 Pro`.
- Проблемы из-за неверного chromedriver:
  - Уберите вручную закрепленный chromedriver из профиля shell; в этом проекте Selenium Manager специально сам выбирает совместимые версии.

## 🧭 Roadmap

Планируемые/вероятные следующие улучшения:
- Добавить автотесты для стабильности селекторов и API-обработчиков.
- Добавить инструменты lint/format и CI-workflows.
- Добавить отслеживаемые PWA icon assets и более надежную стратегию офлайн-кеширования.
- Добавить полноценные многоязычные README-файлы в `i18n/`.
- Добавить метаданные packaging для более простой установки.

## 🤝 Участие

Контрибьюции приветствуются.

Рекомендуемый процесс:
1. Сделайте fork и создайте feature-ветку.
2. Держите изменения узко сфокусированными и добавляйте заметки по воспроизведению/использованию для изменений автоматизации UI.
3. Проверяйте потоки вручную с реальной подключенной сессией Chrome.
4. Откройте PR с деталями поведения до/после.

Если вы меняете селекторы или логику взаимодействия, добавьте конкретный контекст UI Sora, чтобы регрессии было легче диагностировать.

## ❤️ Поддержка / Спонсорство

Ссылки финансирования из `.github/FUNDING.yml`:
- GitHub Sponsors: https://github.com/sponsors/lachlanchen
- Ссылки проекта: https://lazying.art, https://chat.lazying.art, https://onlyideas.art

## 🙏 Благодарности

- Selenium и Selenium Manager за автоматизацию браузера и подбор драйвера.
- Tornado за легковесный асинхронный HTTP/WebSocket-сервис управления.
- Pillow и `pillow-heif` за локальную поддержку конвертации/предпросмотра изображений.

## 🧱 Проверенная стабильная сборка

Если вам нужна стабильная базовая версия, где гарантированно работает прикрепление медиа в storyboard end-to-end (включая кнопки Open Storyboard / Attach Current Path и объединенный поток Apply), используйте коммит:

`c6683ed6d9ee0ac110536352867a26a966e3e275`

## 📄 Лицензия

В текущем снимке репозитория файл лицензии отсутствует (проверено в этом черновике **28 февраля 2026 года**).

Предположение: до добавления лицензии все права остаются у владельца репозитория. Если это не так, добавьте файл `LICENSE` и обновите этот раздел.
